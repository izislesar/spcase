# Local staging override. PostgreSQL remains bound to loopback and is exposed
# only so host-side integration tests can use the same isolated database.
services:
  db:
    ports:
      - "127.0.0.1:${STAGING_DB_PORT:-15432}:5432"
